import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("screen");

export const PRODUCT_CONTAINER_SIZE_X = width * 0.9;
export const PRODUCT_CONTAINER_SIZE_Y = height / 3.5;

export const PRODUCT_WIDTH = width * 0.9;
export const PRODUCT_WIDTH_FULLSIZE = width * 0.9;

export const PRODUCT_HEIGHT = height / 3.5;
